# Single user action with all attached signals (paths, not bytes).

## What it does
Single user action with all attached signals paths not bytes (events module).

Type: rationale
File: app\models\events.py
Graph id: `events_rationale_79`

