# Phase 3 — compile normalized events into a SkillPackage (no runtime execution).

## What it does
Phase compile normalized events into a skill package no runtime execution (build module).

Type: rationale
File: app\compiler\build.py
Graph id: `build_rationale_1`

