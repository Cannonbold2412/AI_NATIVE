# In-memory MVP registry (swap for Redis/DB in production).

## What it does
In memory mvp registry swap for redis db in production (session module).

Type: rationale
File: app\recorder\session.py
Graph id: `session_rationale_152`

