# Returns a new document dict with step merged and meta.version incremented.

## What it does
Returns a new document dict with step merged and meta version incremented (patch module).

Type: rationale
File: app\compiler\patch.py
Graph id: `patch_rationale_22`

