# canonicalize_selectors()

## What it does
Returns normalized selectors plus canonical metadata used by the compiler. Priority order is deterministic: css -> aria -> text_based -> xpath.

Type: code
File: app\pipeline\selectors.py
Graph id: `selectors_canonicalize_selectors`

## Calls
- [[selectors_normalize_selectors_block]]

