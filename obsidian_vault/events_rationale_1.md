# Pydantic models for raw recorder events (multi-signal, high fidelity).

## What it does
Pydantic models for raw recorder events multi signal high fidelity (events module).

Type: rationale
File: app\models\events.py
Graph id: `events_rationale_1`

