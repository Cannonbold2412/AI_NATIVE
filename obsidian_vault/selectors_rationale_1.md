# Selector canonicalization and normalization (deterministic).

## What it does
Selector canonicalization and normalization deterministic (selectors module).

Type: rationale
File: app\pipeline\selectors.py
Graph id: `selectors_rationale_1`

