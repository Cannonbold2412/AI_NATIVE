# Drop consecutive scroll events with identical fingerprints and scroll offsets.

## What it does
Drop consecutive scroll events with identical fingerprints and scroll offsets (dedupe module).

Type: rationale
File: app\pipeline\dedupe.py
Graph id: `dedupe_rationale_9`

