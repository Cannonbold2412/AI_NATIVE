# Owns one browser context + page, drains in-page events into structured JSON.

## What it does
Owns one browser context page drains in page events into structured json (session module).

Type: rationale
File: app\recorder\session.py
Graph id: `session_rationale_27`

