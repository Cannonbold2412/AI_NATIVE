# Collapse redundant consecutive events (deterministic).

## What it does
Collapse redundant consecutive events deterministic (dedupe module).

Type: rationale
File: app\pipeline\dedupe.py
Graph id: `dedupe_rationale_1`

