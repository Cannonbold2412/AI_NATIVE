# Returns normalized selectors plus canonical metadata used by the compiler.

## What it does
Returns normalized selectors plus canonical metadata used by the compiler (selectors module).

Type: rationale
File: app\pipeline\selectors.py
Graph id: `selectors_rationale_17`

