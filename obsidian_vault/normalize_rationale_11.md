# Alias for `run_pipeline` (keeps older imports working).

## What it does
Alias for run pipeline keeps older imports working (normalize module).

Type: rationale
File: app\pipeline\normalize.py
Graph id: `normalize_rationale_11`

