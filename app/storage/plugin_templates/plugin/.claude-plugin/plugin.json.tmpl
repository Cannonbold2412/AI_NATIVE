{
  "name": "{{plugin_name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "mcpServers": {
    "conxa": {
      "command": "node",
      "args": ["{{bootstrap_js}}"]
    }
  }
}
